---
phase: 07-main-page-landing-page-with-teasers-to-drive-app-adoption
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/page.tsx
  - src/middleware.ts
autonomous: true
requirements:
  - LAND-01
  - LAND-02

must_haves:
  truths:
    - "Unauthenticated visitor at / sees the landing page with hero, features, and footer"
    - "Logged-in trainer visiting / is redirected to /trainer"
    - "Logged-in trainee visiting / is redirected to /trainee"
    - "Unauthenticated visitor can access /help without being redirected to /login"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Landing page with hero + features + footer"
      contains: "Replace the spreadsheet"
    - path: "src/middleware.ts"
      provides: "Public path for /help"
      contains: "/help"
  key_links:
    - from: "src/app/page.tsx"
      to: "/signup/trainer"
      via: "Link href"
      pattern: "href.*signup/trainer"
    - from: "src/app/page.tsx"
      to: "/signup/trainee"
      via: "Link href"
      pattern: "href.*signup/trainee"
    - from: "src/app/page.tsx"
      to: "/help"
      via: "Link href in footer"
      pattern: "href.*help"
---

<objective>
Build the Forge landing page and fix middleware to allow public access to /help.

Purpose: Give unauthenticated visitors a marketing entry point that drives trainer and trainee signups. Fix middleware so the FAQ page (built in Plan 02) will be accessible to unauthenticated users.
Output: Complete landing page at `/` with hero, 3 feature cards, and footer. Middleware updated with `/help` in publicPaths.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-main-page-landing-page-with-teasers-to-drive-app-adoption/07-CONTEXT.md
@.planning/phases/07-main-page-landing-page-with-teasers-to-drive-app-adoption/07-UI-SPEC.md
@.planning/phases/07-main-page-landing-page-with-teasers-to-drive-app-adoption/07-RESEARCH.md

<interfaces>
<!-- ForgeLogo component API — reuse in hero -->
From src/components/ForgeLogo.tsx:
```typescript
interface ForgeLogoProps {
  variant?: 'horizontal' | 'icon';
  className?: string;
}
export function ForgeLogo({ variant = 'horizontal', className = 'h-7' }: ForgeLogoProps): JSX.Element;
```

<!-- Middleware structure — line 35 is the publicPaths array to extend -->
From src/middleware.ts:
```typescript
const publicPaths = ['/login', '/signup', '/auth', '/join', '/verify-email', '/invite-invalid', '/forgot-password', '/reset-password'];
const isPublic = publicPaths.some(p => path.startsWith(p));
// Line 57 already handles `/` redirect for authenticated users:
if ((path === '/login' || path === '/') && role) {
  const home = role === 'trainer' ? '/trainer' : '/trainee';
  return NextResponse.redirect(new URL(home, request.url));
}
```

<!-- Login page — reference for card + button + logo patterns -->
From src/app/(auth)/login/page.tsx:
```tsx
<ForgeLogo variant="horizontal" className="h-10" />
<div className="bg-bg-surface border border-border rounded-sm p-8 space-y-6">
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace page.tsx with landing page</name>
  <files>src/app/page.tsx</files>
  <read_first>
    - src/app/page.tsx (current boilerplate to be fully replaced)
    - src/components/ForgeLogo.tsx (component API: variant, className props)
    - src/app/(auth)/login/page.tsx (card + button style reference)
    - src/app/globals.css (design token names)
    - .planning/phases/07-main-page-landing-page-with-teasers-to-drive-app-adoption/07-UI-SPEC.md (visual contract)
  </read_first>
  <action>
Completely replace src/app/page.tsx with the Forge landing page. This is a server component (NO 'use client'). Import Link from 'next/link' and ForgeLogo from '@/components/ForgeLogo'.

The page has exactly 3 sections inside a `<main className="bg-bg-page min-h-screen flex flex-col">`:

**Section 1: Hero**
- `<section>` with `flex-1 flex flex-col items-center justify-center px-4 py-12 md:py-16`
- Content wrapper: `max-w-[640px] mx-auto w-full flex flex-col items-center`
- ForgeLogo: `<ForgeLogo variant="horizontal" className="h-10" />`
- Headline: `<h1 className="text-4xl font-bold text-text-primary text-center mt-8">Replace the spreadsheet.</h1>`
- Subheadline: `<p className="text-base text-text-primary text-center mt-4" style={{ opacity: 0.8 }}>Trainers build plans. Trainees log workouts. In under a minute.</p>`
- CTA row: `<div className="mt-8 flex flex-col gap-3 min-[380px]:flex-row min-[380px]:gap-4">`
  - Primary CTA: `<Link href="/signup/trainer" className="inline-flex items-center justify-center px-6 py-3 rounded-sm font-bold text-base bg-accent text-white hover:bg-accent-hover transition-colors min-h-[44px] cursor-pointer">Sign up as trainer</Link>`
  - Secondary CTA: `<Link href="/signup/trainee" className="inline-flex items-center justify-center px-6 py-3 rounded-sm font-bold text-base border border-border text-text-primary hover:border-accent hover:text-accent transition-colors min-h-[44px] cursor-pointer bg-transparent">Join as trainee</Link>`

**Section 2: Features**
- `<section className="px-4 py-12 md:py-12">` with inner `<div className="max-w-[960px] mx-auto">`
- Section heading: `<h2 className="text-2xl font-bold text-text-primary text-center mb-8">Why Forge?</h2>`
- Grid: `<div className="grid grid-cols-1 md:grid-cols-3 gap-4 md:gap-6">`
- 3 feature cards, each: `<div className="bg-bg-surface border border-border rounded-sm p-6">`
  - Card 1 icon: clipboard/list SVG (28x28, text-accent, stroke-based, viewBox 0 0 24 24, fill="none" stroke="currentColor" strokeWidth="2"). Paths: `<rect x="8" y="2" width="8" height="4" rx="1" ry="1"/><path d="M16 4h2a2 2 0 0 1 2 2v14a2 2 0 0 1-2 2H6a2 2 0 0 1-2-2V6a2 2 0 0 1 2-2h2"/><line x1="12" y1="11" x2="16" y2="11"/><line x1="12" y1="16" x2="16" y2="16"/><line x1="8" y1="11" x2="8.01" y2="11"/><line x1="8" y1="16" x2="8.01" y2="16"/>`
  - Card 1 title: `<h3 className="text-base font-bold text-text-primary mt-4">Build structured plans</h3>`
  - Card 1 desc: `<p className="text-sm text-text-primary mt-2" style={{ opacity: 0.7 }}>Create multi-week programs with sets, reps, and weights.</p>`
  - Card 2 icon: lightning bolt SVG. Paths: `<polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"/>`
  - Card 2 title: `Log workouts in seconds`
  - Card 2 desc: `See last week's results inline and record today's sets fast.`
  - Card 3 icon: trend-up chart SVG. Paths: `<polyline points="22 12 18 8 13 13 9 9 2 16"/><polyline points="22 12 22 8 18 8"/>`  (or similar trending-up line chart)
  - Card 3 title: `Track progress over time`
  - Card 3 desc: `Trainers monitor every session. Trainees watch their lifts grow.`

**Section 3: Footer**
- `<footer className="border-t border-border py-8 px-4">`
- Inner: `<div className="flex flex-col sm:flex-row items-center justify-center gap-4 sm:gap-6 text-sm">`
  - `<Link href="/login" className="text-accent hover:text-accent-hover transition-colors">Already have an account? Log in</Link>`
  - `<Link href="/help" className="text-text-primary hover:text-accent transition-colors">Help / FAQ</Link>`

Remove the `import Image from "next/image"` — no longer needed.
  </action>
  <verify>
    <automated>cd /Users/piotrsowinski/sources/Private/GYM\ AI\ ASSISTANT && grep -c "Replace the spreadsheet" src/app/page.tsx && grep -c "signup/trainer" src/app/page.tsx && grep -c "signup/trainee" src/app/page.tsx && grep -c "/help" src/app/page.tsx && grep -c "ForgeLogo" src/app/page.tsx && grep -c "Build structured plans" src/app/page.tsx && grep -c "next/image" src/app/page.tsx | grep "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/page.tsx contains "Replace the spreadsheet."
    - src/app/page.tsx contains `href="/signup/trainer"` and `href="/signup/trainee"`
    - src/app/page.tsx contains `href="/login"` and `href="/help"`
    - src/app/page.tsx contains `ForgeLogo variant="horizontal" className="h-10"`
    - src/app/page.tsx contains "Build structured plans", "Log workouts in seconds", "Track progress over time"
    - src/app/page.tsx contains "Why Forge?"
    - src/app/page.tsx does NOT contain `import Image` or `next/image`
    - src/app/page.tsx does NOT contain `'use client'`
    - src/app/page.tsx contains `bg-accent text-white` (primary CTA)
    - src/app/page.tsx contains `border border-border text-text-primary` (secondary CTA ghost)
    - src/app/page.tsx contains `min-h-[44px]` on both CTA buttons
    - src/app/page.tsx contains `bg-bg-surface border border-border rounded-sm p-6` (feature cards)
  </acceptance_criteria>
  <done>Landing page renders hero with ForgeLogo + headline + two CTAs, 3 feature cards, and footer with login + help links. No client-side JS. All design tokens from existing globals.css.</done>
</task>

<task type="auto">
  <name>Task 2: Add /help to middleware publicPaths</name>
  <files>src/middleware.ts</files>
  <read_first>
    - src/middleware.ts (current publicPaths array on line 35; verify existing / redirect on line 57)
  </read_first>
  <action>
In src/middleware.ts, add '/help' to the publicPaths array on line 35.

Change from:
```typescript
const publicPaths = ['/login', '/signup', '/auth', '/join', '/verify-email', '/invite-invalid', '/forgot-password', '/reset-password'];
```

To:
```typescript
const publicPaths = ['/login', '/signup', '/auth', '/join', '/verify-email', '/invite-invalid', '/forgot-password', '/reset-password', '/help'];
```

Do NOT modify any other part of the middleware. The existing redirect logic at line 57 already handles `/` for authenticated users — do not touch it.

IMPORTANT: Verify that authenticated users visiting `/help` are NOT caught by any redirect. Tracing the flow:
1. Authenticated user at `/help` — claims exist, no error
2. `/help` does not start with `/trainer` or `/trainee` — cross-role check passes
3. `path === '/login' || path === '/'` — false for `/help` — no redirect
4. Falls through to `return response` — correct, user sees the page

This means authenticated users already work correctly at `/help`. The ONLY fix needed is adding '/help' to publicPaths so UNAUTHENTICATED users are not redirected to /login.
  </action>
  <verify>
    <automated>cd /Users/piotrsowinski/sources/Private/GYM\ AI\ ASSISTANT && grep "'/help'" src/middleware.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/middleware.ts publicPaths array contains '/help'
    - src/middleware.ts still contains the existing line: `if ((path === '/login' || path === '/') && role)` (unchanged)
    - No other lines in middleware.ts are modified beyond the publicPaths array
  </acceptance_criteria>
  <done>Unauthenticated visitors can access /help without being redirected to /login. Authenticated users at /help still pass through correctly (no redirect).</done>
</task>

</tasks>

<verification>
- `grep "Replace the spreadsheet" src/app/page.tsx` returns a match
- `grep "'/help'" src/middleware.ts` returns a match
- `npx next build` completes without errors (if available)
- Landing page at `/` renders three sections: hero, features, footer
- All links use `next/link` Link component (not `<a>` for internal routes)
</verification>

<success_criteria>
1. src/app/page.tsx is a complete landing page with hero (ForgeLogo, headline, subheadline, two CTAs), 3 feature cards, and footer
2. src/middleware.ts has '/help' in publicPaths
3. No new CSS variables, no new dependencies, no 'use client' on page.tsx
4. All design tokens match existing globals.css values
</success_criteria>

<output>
After completion, create `.planning/phases/07-main-page-landing-page-with-teasers-to-drive-app-adoption/07-01-SUMMARY.md`
</output>
