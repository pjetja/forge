---
phase: 09-internationalization
plan: 05
type: execute
wave: 3
depends_on: ["09-02", "09-03", "09-04"]
files_modified: []
autonomous: false
requirements:
  - I18N-01
  - I18N-02
  - I18N-03
  - I18N-04
  - I18N-05
  - I18N-06
  - I18N-07
  - I18N-08
  - I18N-09

must_haves:
  truths:
    - "Polish is displayed by default on first visit (no cookie)"
    - "Switching to English via PL | EN toggle changes all visible text to English"
    - "Switching back to Polish restores all text to Polish"
    - "No raw translation keys (like common.button.save) visible in any page"
    - "Polish diacritics render correctly in Lato font"
    - "lang attribute on HTML element matches active locale"
    - "LanguageSwitcher appears in both trainer and trainee headers on desktop and mobile"
  artifacts: []
  key_links: []
---

<objective>
End-to-end verification of the complete internationalization implementation. Verify that all pages render correctly in both Polish and English, the language switcher works, no raw keys are visible, and Polish diacritics render properly.

Purpose: Catches any missing translations, broken key references, or visual regressions before marking the phase complete.

Output: Verification results confirming i18n works across the entire app.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-internationalization-make-app-multi-language-extract-all-static-labels-into-keys-add-polish-language-and-translations-make-polish-default-add-lang-switcher-in-header/09-CONTEXT.md
@.planning/phases/09-internationalization-make-app-multi-language-extract-all-static-labels-into-keys-add-polish-language-and-translations-make-polish-default-add-lang-switcher-in-header/09-01-SUMMARY.md
@.planning/phases/09-internationalization-make-app-multi-language-extract-all-static-labels-into-keys-add-polish-language-and-translations-make-polish-default-add-lang-switcher-in-header/09-02-SUMMARY.md
@.planning/phases/09-internationalization-make-app-multi-language-extract-all-static-labels-into-keys-add-polish-language-and-translations-make-polish-default-add-lang-switcher-in-header/09-03-SUMMARY.md
@.planning/phases/09-internationalization-make-app-multi-language-extract-all-static-labels-into-keys-add-polish-language-and-translations-make-polish-default-add-lang-switcher-in-header/09-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated verification — build + scan for untranslated strings and missing keys</name>
  <files></files>
  <read_first>
    messages/pl/common.json,
    messages/en/common.json,
    messages/pl/auth.json,
    messages/en/auth.json,
    messages/pl/trainer.json,
    messages/en/trainer.json,
    messages/pl/trainee.json,
    messages/en/trainee.json
  </read_first>
  <action>
1. Run `pnpm build` and verify it succeeds with zero errors.

2. Verify JSON key parity between pl and en for all 4 namespace files:
   - Parse each JSON file
   - Extract all nested key paths (e.g., "common.button.save")
   - Compare pl keys vs en keys for each namespace
   - Report any keys present in one locale but missing in the other

3. Scan all TSX files for remaining hardcoded English strings:
   - Search for common patterns that indicate untranslated strings:
     ```bash
     grep -rn ">[A-Z][a-z].*</" src/app/ src/components/ --include="*.tsx" | grep -v "node_modules" | grep -v ".test." | head -50
     ```
   - Look for string literals in JSX that are NOT inside t() calls, className, href, or other non-translatable attributes
   - Specifically check for common missed strings: "Loading...", "Error", "No data", button labels, headings

4. Verify that `<html lang=` attribute is dynamic:
   ```bash
   grep -n "lang=" src/app/layout.tsx
   ```
   Should show `lang={locale}` not `lang="en"`

5. Verify Lato font includes latin-ext:
   ```bash
   grep -n "subsets" src/app/layout.tsx
   ```
   Should show `['latin', 'latin-ext']`

6. Verify middleware is NOT modified (per RESEARCH.md — no next-intl middleware needed):
   ```bash
   grep -c "next-intl" src/middleware.ts
   ```
   Should return 0.

7. If any issues found, fix them immediately:
   - Missing translations: add to both pl and en JSON files
   - Key mismatches: align keys across locales
   - Remaining hardcoded strings: replace with t() calls
   - Build errors: fix TypeScript issues
  </action>
  <verify>
    <automated>cd /Users/piotrsowinski/sources/Private/GYM\ AI\ ASSISTANT && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - pnpm build succeeds with zero errors
    - All 4 namespace JSON files have identical key structures between pl and en
    - No obvious hardcoded English strings remain in TSX files (grep scan shows zero or near-zero results)
    - src/app/layout.tsx contains "lang={locale}" not "lang=\"en\""
    - src/app/layout.tsx contains "latin-ext"
    - src/middleware.ts does NOT contain "next-intl"
  </acceptance_criteria>
  <done>Build passes, key parity verified across all 8 JSON files, no untranslated strings found in TSX scan, infrastructure correctly configured</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — full i18n walkthrough in both locales</name>
  <files></files>
  <action>
Present the verification checklist to the user. The app should be running via `pnpm dev`. The user will manually walk through the app in both Polish and English to confirm all text is translated, the language switcher works, and no raw keys are visible.
  </action>
  <verify>User confirms "approved" after walkthrough</verify>
  <done>User has verified that both Polish and English locales render correctly across all pages, the language switcher works, no raw keys are visible, and Polish diacritics render properly in Lato font</done>
  <what-built>
Complete internationalization of the Forge app: next-intl installed, all ~100 TSX files have strings extracted to translation keys, Polish and English translations provided for all UI text, PL | EN language switcher in both nav headers, Polish is default locale.
  </what-built>
  <how-to-verify>
1. Open the app in a fresh browser (or clear cookies first)
2. Verify the app loads in **Polish** by default — all text should be in Polish
3. Check the `<html>` element in DevTools — should show `lang="pl"`
4. Look at the trainer nav header — verify **PL | EN** toggle is visible on the right side, PL is highlighted in accent green
5. Click **EN** in the toggle — page should reload and all text should switch to English
6. Check DevTools `<html>` — should now show `lang="en"`
7. Navigate to a few key pages and verify text is in English:
   - Login page
   - Trainer home (trainee roster)
   - Exercise library
   - Plans page
   - Help page
8. Click **PL** to switch back to Polish — verify all text reverts
9. Check the trainee side — log in as a trainee and verify:
   - PL | EN toggle visible in trainee header
   - All tabs (Plans, Exercises, Log, Body Weight) show translated text
   - Workout logging pages show Polish labels
10. On mobile (or DevTools responsive mode), open the hamburger menu sidebar and verify:
    - PL | EN toggle is visible in the sidebar footer
    - Touch targets are adequately sized (at least 44px)
11. Check Polish diacritics: look for characters like a with ogonek, e with ogonek, l with stroke — they should render in the same Lato font as regular text (no font fallback visible)
12. Look for any raw translation keys visible (like `common.button.save` or `trainer.plans.heading`) — there should be none
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- Build succeeds
- Human verification confirms both locales work correctly
- No raw keys visible
- Polish diacritics render properly
- LanguageSwitcher works in both headers (desktop + mobile)
</verification>

<success_criteria>
- pnpm build passes
- Polish renders by default on first visit
- English renders after switching via toggle
- All pages display translated text in both languages
- No raw translation keys visible anywhere
- Polish diacritics render in Lato font
- LanguageSwitcher visible and functional in both nav headers (desktop + mobile sidebar)
- lang attribute on HTML element updates dynamically
</success_criteria>

<output>
After completion, create `.planning/phases/09-internationalization-make-app-multi-language-extract-all-static-labels-into-keys-add-polish-language-and-translations-make-polish-default-add-lang-switcher-in-header/09-05-SUMMARY.md`
</output>
