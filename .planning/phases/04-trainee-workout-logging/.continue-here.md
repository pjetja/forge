---
phase: 04-trainee-workout-logging
task: ui-polish-and-bug-fixes
status: in_progress
last_updated: 2026-03-13T19:01:06.417Z
---

<current_state>
All phase 04 plans are done and verified. We are in an iterative UI/feature polish session. All changes from this session are uncommitted in the working tree. TypeScript is clean (tsc --noEmit passes).
</current_state>

<completed_work>

**Exercise progress summary (completed plans):**
- Created `/trainee/plans/[assignedPlanId]/exercises/[exerciseId]/page.tsx` — progress chart page
- Created `ProgressChart` client component (recharts LineChart, one line per set, colored)
- Added "Exercise Progress" section to trainee plan detail page (always visible, links when plan complete)
- Chart X-axis now shows "Workout 1", "Workout 2" etc.

**Layout improvements:**
- Trainee layout: `max-w-2xl` → `max-w-[1280px]` (matches trainer layout)
- Trainee plan detail page: two-column desktop layout (Workouts left, Exercises right)
- Extracted `CompletedPlanColumns` shared component for completed plan two-column view
- Trainee plan page: uses `CompletedPlanColumns` when planComplete, inline active-plan grid otherwise
- Past plan "Past weeks" label hidden when plan is complete

**Trainer plan summary (new pages):**
- Created `/trainer/trainees/[traineeId]/plans/[assignedPlanId]/page.tsx` — trainer view of completed plan using `CompletedPlanColumns`
- Created `/trainer/trainees/[traineeId]/plans/[assignedPlanId]/exercises/[exerciseId]/page.tsx` — trainer exercise progress chart
- Trainer trainee page: completed past plan cards are now clickable links; terminated plans remain static

**Plan assignment improvements:**
- `PlanPicker`: shows top 3 plans + search input when >3 plans exist; links directly to review page
- Fixed history pre-fill bug: `plan_id` → `source_plan_id` column name
- Replaced `AssignPlanModal` with a full review page at `/trainer/plans/[planId]/assign/[traineeId]/page.tsx`
- Review page: exercises grouped by workout schema, history pre-fill from last completed plan
- `AssignPlanClientPage` simplified to a plain link list (no modal/state)
- Assign page (`/trainer/plans/[planId]/assign`) simplified — removed redundant exercise/history fetching

</completed_work>

<remaining_work>

No specific tasks were left incomplete — the session was wrapping up naturally.

**Potential follow-ups (not requested yet):**
- Apply trainee exercises RLS migration (SQL in previous continue-here) so admin client bypass is not needed
- Trainer workout session viewer (trainer currently can't drill into individual sessions from the plan summary)
- Phase 04.1: UI Polish phase

</remaining_work>

<decisions_made>

- `CompletedPlanColumns` shared component: `getWorkoutHref` is optional — trainer passes nothing (non-clickable sessions), trainee passes the link
- Review page is at `/trainer/plans/[planId]/assign/[traineeId]` — separate from the trainee-selection page
- `PlanPicker` from trainee page now links directly to review page (skips trainee selection step) — better UX since trainer already knows the trainee
- Assign page no longer pre-fetches exercises or history — that's deferred to the review page per-trainee
- Chart per-set lines: `SET_COLORS` array of 6 colors cycling, `connectNulls` handles missing sets
- Summary stats (Start/Finish/Change) use avg across all sets for first vs last session

</decisions_made>

<blockers>

- exercises table RLS is trainer-only. Trainee pages use admin client to read exercise names. SQL fix available but not yet applied to DB:
  ```sql
  CREATE POLICY "trainee_reads_exercises_in_assigned_plans" ON exercises
    FOR SELECT USING (
      id IN (
        SELECT ase.exercise_id FROM assigned_schema_exercises ase
        JOIN assigned_schemas asc ON asc.id = ase.assigned_schema_id
        JOIN assigned_plans ap ON ap.id = asc.assigned_plan_id
        WHERE ap.trainee_auth_uid = (SELECT auth.uid())
      )
    );
  ```

</blockers>

<context>
All changes are uncommitted in the working tree. The session covered: exercise progress charts, desktop two-column layout, trainer plan summary pages, and replacing the assign modal with a full review page. Everything type-checks. The app is in a good state to commit and move to Phase 04.1 (UI Polish).
</context>

<next_action>
Start with: commit all working tree changes with a descriptive message covering the session work, then move to Phase 04.1 UI Polish or continue testing the assign flow.
</next_action>
