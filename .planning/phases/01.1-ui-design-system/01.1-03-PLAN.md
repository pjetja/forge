---
phase: 01.1-ui-design-system
plan: 03
type: execute
wave: 2
depends_on: ["01.1-01"]
files_modified:
  - src/app/(trainer)/layout.tsx
  - src/app/(trainer)/trainer/page.tsx
  - src/app/(trainer)/trainer/_components/InviteDialog.tsx
  - src/app/(trainee)/layout.tsx
  - src/app/(trainee)/trainee/page.tsx
  - src/app/join/[token]/page.tsx
autonomous: false

requirements: []

must_haves:
  truths:
    - "Trainer and trainee app shells have a dark navy top nav bar with the '⚡ Forge' logo in emerald on the left and a ghost 'Sign out' button on the right"
    - "Trainer home page shows trainee roster cards with dark surface cards, emerald avatar circles (not blue), and an emerald 'Invite trainee' button"
    - "InviteDialog modal has a dark surface card with emerald primary button — no white modal background"
    - "Trainee home page shows dark empty state card — no white card"
    - "Join page (all 3 conditional branches: invalid token, conflict, and loading) renders dark navy background and dark surface cards — no gray or white pages"
    - "No blue, white, or gray areas remain in any app shell or join page"
    - "A human confirms all pages render correctly in the browser"
  artifacts:
    - path: "src/app/(trainer)/layout.tsx"
      provides: "Dark nav with ⚡ Forge logo and sign-out, dark navy page bg"
      contains: "⚡ Forge"
    - path: "src/app/(trainee)/layout.tsx"
      provides: "Identical dark nav and dark page bg as trainer layout"
      contains: "⚡ Forge"
    - path: "src/app/(trainer)/trainer/page.tsx"
      provides: "Dark trainer home with dark roster cards and emerald avatars"
      contains: "bg-bg-surface"
    - path: "src/app/(trainer)/trainer/_components/InviteDialog.tsx"
      provides: "Dark modal card with emerald primary button"
      contains: "bg-bg-surface"
    - path: "src/app/(trainee)/trainee/page.tsx"
      provides: "Dark empty state card"
      contains: "bg-bg-surface"
    - path: "src/app/join/[token]/page.tsx"
      provides: "Dark bg + dark cards across all 3 conditional branches"
      contains: "bg-bg-page"
  key_links:
    - from: "src/app/(trainer)/layout.tsx"
      to: "src/app/(auth)/login/actions.ts"
      via: "signOut Server Action in form action"
      pattern: "signOut"
    - from: "src/app/join/[token]/page.tsx"
      to: "globals.css @theme tokens"
      via: "All 3 return branches must use bg-bg-page and bg-bg-surface"
      pattern: "bg-bg-page"
---

<objective>
Restyle all app shell pages (trainer home, trainee home, join page) and their layouts with the dark design system. Ends with a human visual verification checkpoint.

Purpose: Completes the full dark theme rollout across the entire app — every authenticated and unauthenticated page now uses the design system.
Output: 6 restyled files + human confirmation the design looks correct end-to-end.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.1-ui-design-system/01.1-CONTEXT.md
@.planning/phases/01.1-ui-design-system/01.1-RESEARCH.md
@.planning/phases/01.1-ui-design-system/01.1-01-SUMMARY.md
</context>

<interfaces>
<!-- Token classes available from Plan 01 (globals.css @theme inline) -->
<!-- Use these — do NOT hardcode hex values inline -->

Available Tailwind token utilities:
- bg-bg-page        → #0f172a  (page background)
- bg-bg-surface     → #1e293b  (card / surface)
- border-border     → #334155  (all borders)
- text-accent       → #10b981  (emerald text)
- bg-accent         → #10b981  (emerald button fill)
- hover:bg-accent-hover → #34d399
- text-text-primary → #e2e8f0  (off-white body text)

Current trainer layout pattern to replace:
```tsx
<div className="min-h-screen bg-gray-50">
  <header className="bg-white border-b border-gray-200 px-4 py-3 ...">
    <span className="font-semibold text-gray-900">Forge</span>
    <button className="text-sm text-gray-500 hover:text-gray-900 ...">Sign out</button>
```

Target pattern (identical for trainer and trainee layouts):
```tsx
<div className="min-h-screen bg-bg-page">
  <header className="bg-bg-page border-b border-border px-4 py-3 flex items-center justify-between">
    <span className="font-bold text-accent text-lg">⚡ Forge</span>
    <form action={signOut}>
      <button type="submit" className="text-sm text-text-primary hover:text-accent transition-colors">
        Sign out
      </button>
    </form>
  </header>
  <main className="max-w-4xl mx-auto px-4 py-8">{children}</main>
</div>
```

Join page note: This page is OUTSIDE any layout route group. It renders its own full-page wrappers inside EACH conditional branch (3 return statements). Every return must individually use bg-bg-page and bg-bg-surface. Do not assume a shared layout handles the background.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Restyle trainer and trainee app shell layouts + trainer home + trainee home</name>
  <files>
    src/app/(trainer)/layout.tsx,
    src/app/(trainee)/layout.tsx,
    src/app/(trainer)/trainer/page.tsx,
    src/app/(trainee)/trainee/page.tsx
  </files>
  <action>
    **src/app/(trainer)/layout.tsx and src/app/(trainee)/layout.tsx — identical changes:**

    Replace both with the dark nav pattern. The signOut import already exists in both files — keep it.

    ```tsx
    import { signOut } from '@/app/(auth)/login/actions';

    export default function TrainerLayout({ children }: { children: React.ReactNode }) {
      return (
        <div className="min-h-screen bg-bg-page">
          <header className="bg-bg-page border-b border-border px-4 py-3 flex items-center justify-between">
            <span className="font-bold text-accent text-lg">⚡ Forge</span>
            <form action={signOut}>
              <button
                type="submit"
                className="text-sm text-text-primary hover:text-accent transition-colors"
              >
                Sign out
              </button>
            </form>
          </header>
          <main className="max-w-4xl mx-auto px-4 py-8">{children}</main>
        </div>
      );
    }
    ```

    For trainee layout, use `max-w-2xl` for main (was already 2xl in original — keep it). Component name stays `TraineeLayout`.

    **src/app/(trainer)/trainer/page.tsx — Trainer home page:**

    - Page heading: `text-2xl font-bold text-text-primary`
    - "Invite trainee" / invite button: `bg-accent hover:bg-accent-hover text-white rounded-sm px-4 py-2 text-sm font-medium transition-colors`
    - Trainee roster cards: `bg-bg-surface border border-border rounded-sm p-4` — remove any shadow or rounded-lg
    - Trainee avatar circle (initials): replace `bg-blue-100 text-blue-700` with `bg-accent/20 text-accent` — emerald tint bg, emerald text
    - Trainee name text: `text-text-primary font-medium`
    - Any subtext (email, joined date): `text-sm text-text-primary` (same color, smaller size — no gray muting)
    - Empty state (no trainees): `bg-bg-surface border border-border rounded-sm p-12 text-center space-y-3` with heading `text-text-primary font-medium` and body `text-sm text-text-primary`
    - Remove all: bg-white, shadow-sm, rounded-xl, rounded-lg, text-gray-*, border-gray-*, bg-blue-*, text-blue-*

    **src/app/(trainee)/trainee/page.tsx — Trainee home page:**

    - Page heading: `text-2xl font-bold text-text-primary`
    - Empty state card (no plan assigned): `bg-bg-surface border border-border rounded-sm p-12 text-center space-y-3`
    - Heading in card: `text-text-primary font-medium`
    - Body text in card: `text-sm text-text-primary`
    - Remove: bg-white, shadow-sm, rounded-xl, rounded-lg, any gray or blue classes
  </action>
  <verify>
    Run `npx next dev`. Log in as trainer (or check layout in DevTools without logging in by visiting protected pages directly if session exists).
    - http://localhost:3000/trainer — dark nav with ⚡ Forge in emerald, dark page bg, dark roster cards with emerald avatar circles
    - http://localhost:3000/trainee — dark nav with ⚡ Forge in emerald, dark page bg, dark empty state card
    No white, gray, or blue elements visible.
  </verify>
  <done>Both app shell layouts show dark nav with emerald ⚡ Forge logo and ghost sign-out button. Trainer home renders dark cards with emerald avatars. Trainee home renders dark empty state card. Zero legacy light-theme classes remain in these 4 files.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle InviteDialog modal and join page (all conditional branches)</name>
  <files>
    src/app/(trainer)/trainer/_components/InviteDialog.tsx,
    src/app/join/[token]/page.tsx
  </files>
  <action>
    **src/app/(trainer)/trainer/_components/InviteDialog.tsx:**

    The modal has a backdrop (`bg-black/40` or similar) and a card inside. Keep the backdrop as-is — `bg-black/40` is already dark-mode compatible.

    Modal card: replace `bg-white rounded-xl shadow-lg` (or similar) with `bg-bg-surface border border-border rounded-sm`

    Inside modal:
    - Heading: `text-xl font-bold text-text-primary`
    - Description text: `text-sm text-text-primary`
    - Generated link/code display area: `bg-bg-page border border-border rounded-sm px-3 py-2 text-sm text-text-primary font-mono break-all`
    - Copy button or "Generate" primary button: `bg-accent hover:bg-accent-hover text-white rounded-sm px-4 py-2 text-sm font-medium transition-colors`
    - Secondary/cancel button (if present): No background, `text-text-primary hover:text-accent text-sm transition-colors` (ghost)
    - Remove: bg-white, rounded-xl, rounded-lg, shadow-*, border-gray-*, text-gray-*, bg-blue-*, text-blue-*
    - Close (X) button: `text-text-primary hover:text-accent transition-colors`

    **src/app/join/[token]/page.tsx — CRITICAL: this page has multiple conditional return branches.**

    Read the file carefully. It likely has 3-4 `return (...)` statements for: invalid/expired invite, already-connected conflict, error, and the loading/processing state. Each return renders its own full-page wrapper.

    For EVERY return branch, apply the same pattern:
    - Full page wrapper: `min-h-screen flex items-center justify-center bg-bg-page p-4`
    - Card: `bg-bg-surface border border-border rounded-sm p-8 max-w-md w-full space-y-4`
    - Heading: `text-xl font-bold text-text-primary`
    - Body text: `text-sm text-text-primary`
    - Links or action buttons: `text-accent hover:text-accent-hover`
    - Error icon or status indicator: keep existing emoji/icon, just update surrounding text colors
    - Remove: bg-gray-50, bg-white, rounded-xl, rounded-lg, shadow-*, text-gray-*, text-blue-*

    Do NOT assume any return branch inherits a layout wrapper — each one must be self-contained with the dark background.
  </action>
  <verify>
    1. On http://localhost:3000/trainer, click the invite button — InviteDialog should open with dark card, no white modal. Emerald primary button visible.
    2. Visit http://localhost:3000/join/invalid-token — should show dark page background with dark error card, not gray background.
    3. No white or gray backgrounds on any of these pages.
  </verify>
  <done>InviteDialog modal is dark-surfaced with emerald buttons. All conditional branches of join/[token]/page.tsx render dark backgrounds and dark surface cards. No bg-white or bg-gray-50 remains in either file.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification — confirm dark design system across all pages</name>
  <action>All restyling automated in Tasks 1 and 2. Human reviews the visual result in the browser to confirm correctness before the phase is marked complete.</action>
  <verify>Human visually inspects all pages per the checklist below.</verify>
  <done>Human types "approved" confirming consistent dark design system visible across all pages.</done>
  <what-built>
    Full dark design system applied across all 15 pages and components:
    - Design tokens in globals.css (Plan 01)
    - Lato font via next/font (Plan 01)
    - Auth pages: login, signup trainer, signup trainee, verify-email (Plan 02)
    - Auth form components: LoginForm, SignupForm (Plan 02)
    - App shells: trainer layout, trainee layout (Plan 03, Task 1)
    - App pages: trainer home, trainee home (Plan 03, Task 1)
    - InviteDialog modal (Plan 03, Task 2)
    - Join page all branches (Plan 03, Task 2)
  </what-built>
  <how-to-verify>
    With `npx next dev` running, visit and check each page:

    1. http://localhost:3000/login
       - Dark navy full page background
       - Dark surface card centered
       - Lato font visible (not Arial or Geist)
       - Google button: dark border, off-white text
       - "or" divider: NO white rectangle — label background matches card
       - Email/password inputs: dark surface, emerald border on focus
       - Sign in button: solid emerald, white text
       - "Don't have an account?" link: emerald color

    2. http://localhost:3000/signup/trainer (and /signup/trainee)
       - Same dark card structure as login
       - Same input/button dark styling

    3. http://localhost:3000/verify-email
       - Dark card, off-white text, emerald resend link

    4. http://localhost:3000/trainer (logged in as trainer)
       - Dark nav: ⚡ Forge in emerald on left, "Sign out" on right
       - Dark navy page background
       - Trainee roster cards: dark surface, emerald avatar circles (not blue)
       - Invite button: emerald
       - Click invite: InviteDialog opens as dark modal on dark backdrop

    5. http://localhost:3000/trainee (logged in as trainee)
       - Same dark nav as trainer
       - Dark empty state card

    6. http://localhost:3000/join/invalid-token-test
       - Dark navy page background (not gray)
       - Dark surface card with error message

    Confirm: No white or gray backgrounds anywhere. No blue buttons or links anywhere. All text is off-white (#e2e8f0) — readable on dark backgrounds.
  </how-to-verify>
  <resume-signal>Type "approved" if the dark design system looks correct across all pages, or describe any specific pages/elements that need fixing.</resume-signal>
</task>

</tasks>

<verification>
Full design system rollout verified:
- All 15 files use dark token classes — no hardcoded grays, blues, or whites remain
- ⚡ Forge logo appears in emerald on dark nav across all app shells
- Lato font confirmed loading in browser network tab
- Every auth page renders a dark surface card on dark navy
- Every app page renders dark nav + dark content cards
- InviteDialog and join page branches all dark
- Human-verified in browser
</verification>

<success_criteria>
Phase 01.1 UI Design System complete. A user can navigate through login → signup → verify-email → trainer/trainee home → invite flow and see a consistent dark navy interface with emerald accents throughout. No legacy light-theme elements remain anywhere in the app.
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-ui-design-system/01.1-03-SUMMARY.md`
</output>
