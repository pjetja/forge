---
phase: 03-plan-builder
plan: "06"
type: execute
wave: 4
depends_on:
  - "03-02"
  - "03-03"
  - "03-04"
  - "03-05"
files_modified: []
autonomous: false
requirements:
  - PLAN-01
  - PLAN-02
  - PLAN-03
  - PLAN-04
  - PLAN-05
  - PLAN-06
  - PLAN-07

must_haves:
  truths:
    - "Trainer can create a named multi-week plan and see it in the Plans list"
    - "Trainer can open a plan, see week tabs, and name workout slots"
    - "Trainer can open a schema and add exercises with sets/reps/weight; drag-to-reorder works"
    - "Trainer can assign a plan to a trainee and the trainee card shows the plan"
    - "Trainer can duplicate a plan from the Plans list and the copy appears independently"
    - "Trainer can edit exercises on an assigned plan from the trainee detail page"
    - "Nav shows 3 tabs; only one tab is active at a time"
  artifacts:
    - path: "src/lib/db/migrations/0003_plans.sql"
      provides: "Migration file verified to exist (must be applied to Supabase manually)"
    - path: "src/app/(trainer)/trainer/_components/NavHeader.tsx"
      provides: "3-tab nav in the app shell"
    - path: "src/app/(trainer)/trainer/plans/page.tsx"
      provides: "Plans list with duplicate button"
    - path: "src/app/(trainer)/trainer/plans/[planId]/schemas/[schemaId]/page.tsx"
      provides: "Schema editor with DnD exercises"
    - path: "src/app/(trainer)/trainer/plans/[planId]/assign/page.tsx"
      provides: "Assignment flow with review step"
  key_links:
    - from: "NavHeader.tsx"
      to: "/trainer, /trainer/plans, /trainer/exercises routes"
      via: "3 Link elements with correct active class"
      pattern: "Plans.*Exercise Library.*Trainees"
---

<objective>
Human verification of the complete Plan Builder feature — all 7 requirements validated end-to-end through the UI.

Purpose: Catch visual issues, interaction regressions, and functional gaps before the phase is marked complete.
Output: Approval or issue list from the trainer's workflow walkthrough.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-plan-builder/03-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Plan Builder feature:
    - DB migration (0003_plans.sql) with 6 tables + 2 RPC functions + RLS
    - 3-tab nav (Trainees, Plans, Exercise Library) with correct active states
    - Trainees landing page showing plan names per trainee
    - Trainee detail page with current plan, edit link, and past plans
    - Plans list with plan cards + Duplicate button
    - Plan creation form (name, weeks, workouts/week)
    - Plan template editor with week tabs and workout slot cards
    - Schema editor with drag-and-drop exercise list and per-set weight toggle
    - Exercise picker modal for adding exercises to a schema
    - Assign plan flow with trainee picker, active-plan warning, and weight review step
    - Edit assigned plan page (immediate live edits, bumps plan_updated_at)
  </what-built>
  <how-to-verify>
    BEFORE TESTING: Apply the database migration first.
    1. Open Supabase SQL Editor for the project
    2. Paste the contents of src/lib/db/migrations/0003_plans.sql
    3. Click Run — verify no errors

    Then run the dev server: `pnpm dev`
    Visit http://localhost:3000/trainer and log in as a trainer.

    **Test 1 — Navigation (PLAN-01 prerequisite)**
    - Confirm bottom nav shows: Trainees | Plans | Exercise Library
    - Click each tab — confirm only one is highlighted at a time
    - Click Trainees: URL = /trainer (not /trainer/trainees), Trainees tab active
    - Click Plans: /trainer/plans, Plans tab active
    - Click Exercise Library: /trainer/exercises, Exercise Library tab active

    **Test 2 — Create a plan (PLAN-01)**
    - Click Plans tab → click "+ New plan"
    - Fill: Name = "Test 8-Week Plan", Weeks = 8, Workouts/week = 3
    - Click "Create plan" — should redirect to /trainer/plans/[id] (the plan editor)
    - Go back to Plans list: plan card shows "8 weeks · 3 workouts/week"

    **Test 3 — Plan template editor and schema creation (PLAN-02, PLAN-03)**
    - Open the plan editor for "Test 8-Week Plan"
    - Confirm 8 week tabs (Week 1 ... Week 8) appear
    - Click between tabs — all tabs show the SAME 3 workout slots (no per-week variation)
    - Click "+ Add Workout 1 (e.g. Push Day)" → type "Push Day" → Save
    - Slot 1 now shows "Push Day" with "Edit exercises" link
    - Click Week 5 tab — still shows "Push Day" in slot 1 (same template)

    **Test 4 — Schema editor and exercises (PLAN-04)**
    - Click "Edit exercises" on Push Day
    - URL = /trainer/plans/[id]/schemas/[id]
    - Click "+ Add Exercise" — exercise picker modal opens
    - Search for an exercise, click it — exercise appears in the list
    - Confirm sets/reps default to 3/10
    - Change sets to 4 — click elsewhere — confirm sets stays 4 after
    - Enter a target weight (e.g. 80) — blur — should persist
    - Click "Per-set weights" — row expands to show 4 weight inputs (one per set)
    - Change S2 weight to 85 — blur — should persist
    - Add a second exercise to the schema
    - Drag the second exercise above the first — list reorders correctly
    - Drag handle only — number inputs should NOT trigger drag

    **Test 5 — Assign plan to trainee (PLAN-05)**
    - Go to Plans list, open "Test 8-Week Plan"
    - Click "Assign to trainee"
    - Select a trainee from the list
    - Review modal opens — shows exercise with weight pre-filled from template (or "No previous data")
    - Adjust one weight in the modal
    - Click "Assign plan to [trainee]"
    - Should redirect to /trainer/trainees/[id]
    - Trainee detail shows "Test 8-Week Plan" with "Pending" badge

    **Test 6 — Trainees page shows plan info (PLAN-05 UX)**
    - Go to Trainees tab (/trainer)
    - Trainee card shows "Pending: Test 8-Week Plan" below trainee name
    - Click the card → goes to /trainer/trainees/[id]

    **Test 7 — Edit assigned plan (PLAN-07)**
    - On trainee detail page, click "Edit plan exercises"
    - URL = /trainer/trainees/[id]/assigned-plans/[id]/edit
    - Shows exercise list with editable sets/reps/weight inputs
    - Change a weight value and blur — "Changes saved" toast appears

    **Test 8 — Duplicate plan (PLAN-06)**
    - Go to Plans list
    - Click "Duplicate" on "Test 8-Week Plan"
    - Page refreshes, new plan "Copy of Test 8-Week Plan" appears
    - Open the copy — it has its own schema copies (independent)

    Report any issues with steps, visual problems, or unexpected behavior.
  </how-to-verify>
  <resume-signal>Type "approved" if all 8 tests pass, or describe the issues found.</resume-signal>
</task>

</tasks>

<verification>
Human verification of all 7 Phase 3 requirements through end-to-end UI walkthrough.
</verification>

<success_criteria>
All 8 test scenarios pass: nav tabs, plan creation, week template view, schema editor with DnD, exercise picker, plan assignment with review step, trainee page showing plan, assigned plan editing, and plan duplication.
</success_criteria>

<output>
After completion, create `.planning/phases/03-plan-builder/03-06-SUMMARY.md`
</output>
