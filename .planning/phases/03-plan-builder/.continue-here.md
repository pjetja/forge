---
phase: 03-plan-builder
task: UAT-fixes
total_tasks: ongoing
status: in_progress
last_updated: 2026-03-13T10:03:49.764Z
---

<current_state>
Mid-batch UAT fix round. All 6 execution plans (03-01 through 03-06) are COMPLETE with SUMMARYs.
We are in the UAT iteration loop — user tested, found issues, we fix them, repeat.
Currently mid-way through the THIRD round of UAT fixes. Context hit 94% during implementation.

The LAST change made (incomplete — write was interrupted by context warning):
- `PlanDetailHeader.tsx` was being rewritten to remove the tags section (now a separate component)
  and update text to "3 weeks · 2 workouts per week" full text. The write COMPLETED successfully
  but remaining changes in the batch were NOT done.
</current_state>

<completed_work>

## Round 1 UAT fixes (committed: 84f5642)
- Exercise appears in list immediately after add (useEffect syncs initialItems)
- Per-set weights now save correctly (removed JSON.stringify double-encoding)
- Number inputs select-all on focus (fixes "01" bug on mobile)
- DnD now works on mobile (TouchSensor delay:250ms + tolerance:5px)
- Remove exercise requires confirmation before deleting
- Exercise picker is full-screen on mobile (92vh panel from bottom)
- Exercise picker has muscle group filter chips

## Round 2 UAT fixes (committed: 792bc47)
- Plan tags (text[]) + notes (text) + status columns → migration 0004_plan_metadata.sql
- Create plan form: tag chip input + notes textarea
- Plans list: tag filter chips; only shows active plans
- PlanCard: tags shown, Duplicate + Delete/Archive with confirm step
- Smart delete: active trainees → archive; no trainees → hard delete
- Plan detail: active trainees list (always visible)
- Plan detail: notes textarea with auto-save on blur
- Exercise picker: min-h prevents layout shift after filtering

## Round 2 migration fallback (committed: e96df19)
- Plans list, plan detail, createPlan, updatePlan all fall back gracefully when migration 0004 not applied
- Yellow banner shown prompting user to apply migration

## Round 3 fixes - batch 1 (committed: 9634970)
- DnD mobile: touch-action:none on drag handle (critical @dnd-kit fix)
- PlanCard: SVG icons replace text; duplicate navigates to copy for immediate rename
- PlanDetailHeader: inline name editing, tag chip editor, duplicate/delete icon buttons
- Trainees section always visible (shows "No trainees assigned yet")

## Round 3 fixes - batch 2 - PARTIALLY DONE
- `PlanDetailHeader.tsx` rewritten: tags section REMOVED (to be standalone PlanTagsEditor),
  full text "3 weeks · 2 workouts per week", cleaner structure — COMMITTED IN DIRTY STATE
  (file was written but not committed yet, see git diff)
</completed_work>

<remaining_work>

The current round of UAT requests (from user's last message) — items NOT yet done:

1. **PlanTagsEditor** — New standalone client component (extracted from PlanDetailHeader).
   Tags editor with chip input, add/remove, saves to DB on change.

2. **Plan detail page section order** — Update `plans/[planId]/page.tsx`:
   - Section 1: PlanDetailHeader (name, actions, weeks/workouts)
   - Section 2: Two-column grid — left: PlanTagsEditor, right: PlanNotesEditor (only if hasMeta)
   - Section 3: Workouts (single week, no tabs — see PlanWeekView change below)
   - Section 4: Trainees (always visible) + "Assign to trainee" button

3. **PlanWeekView** — Remove week tabs entirely. Trainer sees single workout list.
   Replace "Edit exercises" text link with a pencil/arrow icon button.

4. **PlansClient** — Always show filter bar (even with 0 tags — show just "All" chip).
   Currently hidden when `allTags.length === 0`.

5. **PlanCard** — Full text: "3 weeks · 2 workouts per week" (not "3w · 2x/week").
   Icons already bigger (18px done in batch 1).

6. **SchemaExerciseRow** — Replace `confirm()` dialog with inline custom confirm UI.
   When user taps ×, show inline "Remove [name]? [Remove] [Cancel]" inside the row.
   Per-set weights: use string state internally (`""` not `0`) so typing "4" gives "4" not "04".

7. **PlanWeekView "Edit exercises"** — Change text link to icon (pencil SVG, same style as
   other row icons: `p-2 opacity-50 hover:opacity-100`).

8. **Figma scripter** — After code changes, write a Figma scripter script that creates
   the custom delete confirm modal component in Figma.

9. **MEMORY.md** — Update with icon pattern convention:
   "All row/list action buttons use SVG icons, p-2 padding, opacity-50 hover:opacity-100,
   18px size. No text buttons for row actions."
</remaining_work>

<decisions_made>
- Tags moved OUT of PlanDetailHeader into standalone PlanTagsEditor component
- Two-column layout (tags | notes) only shown when migration 0004 is applied (hasMeta=true)
- Single week view for trainer (week tabs removed — all weeks have same template)
- Per-set weights: use string state `""` as initial value, parse only on save
- Inline delete confirm (not browser confirm, not overlay modal) — show in-row "Remove? [yes] [cancel]"
- Icon standard: SVG, 18px, p-2 padding, opacity-50 hover:opacity-100, hover color varies by action
</decisions_made>

<blockers>
- Migration 0004 may not be applied by user yet — all new plan metadata features have graceful fallback
- Context exhausted mid-implementation — PlanDetailHeader was rewritten but remaining 8 items not started
</blockers>

<context>
We are doing UAT iteration on Phase 3 (Plan Builder). All execution plans are done.
The user is testing on mobile and desktop, finding UX issues, we fix them iteratively.

The overall pattern for this codebase:
- Next.js 14 App Router, Server Components for data, Client Components for interactivity
- Supabase for DB, RLS policies, server actions in `actions.ts` files
- Tailwind with custom tokens: bg-bg-surface, bg-bg-page, text-text-primary, border-border, accent, etc.
- Icons: inline SVGs, 18px, no icon library
- Mobile-first: full-screen modals from bottom on mobile (sm: breakpoint for desktop)

The git state: PlanDetailHeader.tsx is modified but NOT committed (it's in `git diff HEAD`).
Before writing new files, need to commit the current PlanDetailHeader change.
</context>

<next_action>
1. Run: `git add src/app/(trainer)/trainer/_components/PlanDetailHeader.tsx && git commit -m "fix(03-06): extract tags from header, full week/workout text"`
2. Create `PlanTagsEditor.tsx` (standalone chip editor)
3. Update `plans/[planId]/page.tsx` for new section order + two-col layout
4. Update `PlanWeekView.tsx` to remove week tabs
5. Update `PlansClient.tsx` to always show filter bar
6. Update `PlanCard.tsx` for full text
7. Update `SchemaExerciseRow.tsx` for inline delete confirm + string-based per-set weights
8. Write Figma scripter script
9. Update MEMORY.md with icon convention
</next_action>
