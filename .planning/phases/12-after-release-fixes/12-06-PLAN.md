---
phase: 12-after-release-fixes
plan: "06"
title: Drag and drop on assigned schema editor
type: execute
wave: 6
depends_on: ["12-05"]
requirements:
  - FIX-13
priority: NICE TO HAVE
autonomous: true
files_to_modify:
  - src/app/(trainer)/trainer/trainees/[traineeId]/assigned-plans/[assignedPlanId]/edit/EditAssignedPlanClient.tsx
  - src/app/(trainer)/trainer/trainees/[traineeId]/assigned-plans/[assignedPlanId]/edit/page.tsx
  - src/app/(trainer)/trainer/trainees/actions.ts (or nearby actions file)
---

<objective>
Add drag-and-drop exercise reordering to the assigned plan schema editor at `/trainer/trainees/[id]/assigned-plans/[id]/edit`.

The plan template schema editor (`/trainer/plans/[id]/schemas/[id]`) already uses `@dnd-kit/core` + `@dnd-kit/sortable` for DnD. Mirror that pattern here.
</objective>

<tasks>

## Task 1 — Inspect current assigned schema editor

<read_first>
- src/app/(trainer)/trainer/trainees/[traineeId]/assigned-plans/[assignedPlanId]/edit/EditAssignedPlanClient.tsx
- src/app/(trainer)/trainer/trainees/[traineeId]/assigned-plans/[assignedPlanId]/edit/page.tsx
</read_first>

Read `EditAssignedPlanClient.tsx` to understand the current exercise list rendering. Identify: how exercises are listed, how reorder server action (if any) is structured, and what the exercise row component is.

<acceptance_criteria>
- Inspection complete; note in SUMMARY: exercise row component name, prop holding exercise list, whether sort_order exists
- `grep -n 'sort_order' src/app/(trainer)/trainer/trainees/[traineeId]/assigned-plans/[assignedPlanId]/edit/EditAssignedPlanClient.tsx` — note result
</acceptance_criteria>

## Task 2 — Add DnD to assigned schema exercise list

<read_first>
- src/app/(trainer)/trainer/trainees/[traineeId]/assigned-plans/[assignedPlanId]/edit/EditAssignedPlanClient.tsx
- src/app/(trainer)/trainer/plans (grep for DndContext to find the reference DnD schema editor)
</read_first>

**Pattern:** Copy from `src/app/(trainer)/trainer/plans/[planId]/schemas/[schemaId]/page.tsx` DnD setup. The assigned schema editor should use identical `DndContext` + `SortableContext` + drag handle setup.

Steps:

1. Wrap the exercise list in `DndContext` and `SortableContext` from `@dnd-kit`
2. Make each exercise row a sortable item with a drag handle (⠿ icon, same style as plan template)
3. On `onDragEnd`, call a server action `reorderAssignedSchemaExercises(assignedSchemaId, orderedIds)` that updates `sort_order` for each `assigned_schema_exercises` row
4. Add the reorder server action if it doesn't already exist

**Server action signature:**

```ts
export async function reorderAssignedSchemaExercises(
  assignedSchemaId: string,
  orderedIds: string[],
): Promise<void>;
```

<acceptance_criteria>
- `grep -n 'DndContext' src/app/(trainer)/trainer/trainees/[traineeId]/assigned-plans/[assignedPlanId]/edit/EditAssignedPlanClient.tsx` exits 0
- `grep -rl 'reorderAssignedSchema' src/app/(trainer)/trainer/trainees/` exits 0 (server action created)
</acceptance_criteria>

## Task 3 — Verify no dependency regression

<read_first>
- package.json
</read_first>

Confirm `@dnd-kit/core` and `@dnd-kit/sortable` are already in `package.json` (they should be from Phase 3). No new packages needed.

<acceptance_criteria>
- `grep -n '@dnd-kit/core' package.json` exits 0 (already present, no new install)
</acceptance_criteria>

</tasks>

<must_haves>
<truths> - Exercises in the assigned schema editor can be reordered by dragging the drag handle (⠿) - After a drag, the new order is persisted to the database via a server action - The drag handle is touch-safe (TouchSensor with delay constraint) - @dnd-kit packages are not added again (already present)
</truths>
</must_haves>
