---
phase: 12-after-release-fixes
plan: "05"
title: Loading states + Gravatar docs
type: execute
wave: 5
depends_on: ["12-04"]
requirements:
  - FIX-08
  - FIX-10
autonomous: true
files_to_modify:
  - src/app/(trainee)/trainee/loading.tsx (CREATE)
  - src/app/(trainer)/trainer/loading.tsx (CREATE)
  - src/app/(trainer)/trainer/trainees/[traineeId]/loading.tsx (CREATE)
  - src/app/(trainer)/trainer/profile/page.tsx
  - src/app/(trainee)/trainee/profile/page.tsx
---

<objective>
Two improvements:

1. **Loading states** — Add `loading.tsx` route-level loading files (Next.js convention) for the slowest routes, showing a skeleton layout while server data is fetched. Priority routes: trainee home, trainer trainee-detail, trainer home (trainee roster).

2. **Gravatar documentation** — Add a short explainer in the user profile page(s) explaining how profile photos are sourced from Gravatar and how to set them up. Link to gravatar.com.
   </objective>

<tasks>

## Task 1 — Loading skeleton files

<read_first>
- src/app/(trainee)/trainee/page.tsx (understand page shape for skeleton)
- src/app/(trainer)/trainer/page.tsx
- src/app/(trainer)/trainer/trainees/[traineeId]/page.tsx
</read_first>

For each target route, create a `loading.tsx` in the same directory as `page.tsx`. The loading component renders a skeleton that matches the shape of the real page (no spinners — use skeleton placeholder blocks).

Target routes (highest visible impact on Vercel free plan):

- `src/app/(trainee)/trainee/loading.tsx` — skeleton for the 4-tab home page
- `src/app/(trainer)/trainer/loading.tsx` — skeleton for the trainee roster
- `src/app/(trainer)/trainer/trainees/[traineeId]/loading.tsx` — skeleton for trainee detail

Skeleton style: use `bg-border rounded animate-pulse` placeholder blocks. Match approximate heights of the real content areas (heading bar, card rows, tab switcher).

<acceptance_criteria>
- `ls src/app/(trainee)/trainee/loading.tsx` exits 0
- `ls src/app/(trainer)/trainer/loading.tsx` exits 0
- `ls src/app/(trainer)/trainer/trainees/[traineeId]/loading.tsx` exits 0
- `grep -n 'animate-pulse' src/app/(trainee)/trainee/loading.tsx` exits 0
</acceptance_criteria>

## Task 2 — Gravatar documentation

<read_first>
- src/app/(trainer)/trainer/profile/page.tsx
- src/app/(trainee)/trainee/profile/page.tsx
- messages/en/trainer.json
- messages/pl/trainer.json
</read_first>

**Location:** Add to the trainer and trainee profile pages — a small info note near the avatar display: "Your profile photo is sourced from Gravatar. [Set up your photo →](https://gravatar.com)"

**Files:**

- `src/app/(trainer)/trainer/profile/page.tsx` — add Gravatar info note near avatar section
- `src/app/(trainee)/trainee/profile/page.tsx` — same

**i18n keys** to add in both locales:

- `"profile.gravatarNote"`: EN: `"Profile photo from"`, PL: `"Zdjęcie profilowe z"` — rendered as `"{t('profile.gravatarNote')} <a href='https://gravatar.com'>Gravatar</a>"`

<acceptance_criteria>
- `grep -n 'gravatar' src/app/(trainer)/trainer/profile/page.tsx` exits 0
- `grep -n 'gravatar' src/app/(trainee)/trainee/profile/page.tsx` exits 0
- `grep -n 'gravatarNote' messages/en/trainer.json` exits 0
</acceptance_criteria>

</tasks>

<must_haves>
<truths> - Navigating to /trainee shows a skeleton loading state while the page loads (no blank screen) - Navigating to /trainer shows a skeleton loading state - Navigating to a trainee detail page shows a skeleton loading state - Both trainer and trainee profile pages show a Gravatar attribution note with a link to gravatar.com
</truths>
</must_haves>
