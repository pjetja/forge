---
phase: 01.2-logo-generation
plan: 02
type: execute
wave: 2
depends_on:
  - "01.2-01"
files_modified:
  - src/app/(trainer)/layout.tsx
  - src/app/(trainee)/layout.tsx
  - src/app/(auth)/login/page.tsx
  - src/app/(auth)/signup/trainer/page.tsx
  - src/app/(auth)/signup/trainee/page.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "The nav bar in trainer layout shows ForgeLogo horizontal variant (no ⚡ Forge text)"
    - "The nav bar in trainee layout shows ForgeLogo horizontal variant (no ⚡ Forge text)"
    - "The login page shows ForgeLogo horizontal variant above the dark surface card"
    - "The trainer signup page shows ForgeLogo horizontal variant above the dark surface card"
    - "The trainee signup page shows ForgeLogo horizontal variant above the dark surface card"
    - "No emoji or plain text fallback for the logo remains in the codebase"
  artifacts:
    - path: "src/app/(trainer)/layout.tsx"
      provides: "Trainer nav with ForgeLogo"
      contains: "ForgeLogo"
    - path: "src/app/(trainee)/layout.tsx"
      provides: "Trainee nav with ForgeLogo"
      contains: "ForgeLogo"
    - path: "src/app/(auth)/login/page.tsx"
      provides: "Login page with logo above card"
      contains: "ForgeLogo"
    - path: "src/app/(auth)/signup/trainer/page.tsx"
      provides: "Trainer signup with logo above card"
      contains: "ForgeLogo"
    - path: "src/app/(auth)/signup/trainee/page.tsx"
      provides: "Trainee signup with logo above card"
      contains: "ForgeLogo"
  key_links:
    - from: "src/app/(trainer)/layout.tsx"
      to: "src/components/ForgeLogo"
      via: "import { ForgeLogo } from '@/components/ForgeLogo'"
      pattern: "ForgeLogo"
    - from: "src/app/(auth)/login/page.tsx"
      to: "src/components/ForgeLogo"
      via: "import { ForgeLogo } from '@/components/ForgeLogo'"
      pattern: "ForgeLogo"
---

<objective>
Integrate the approved ForgeLogo component into all key touchpoints: both nav layouts and all three auth pages.

Purpose: The logo design is approved (Plan 01 checkpoint passed). This plan performs the surgical replacements across 5 files to complete the phase.

Output: All `⚡ Forge` placeholders replaced with the designed SVG logo. The app reads as a real product with a branded identity.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.2-logo-generation/01.2-CONTEXT.md
@.planning/phases/01.2-logo-generation/01.2-RESEARCH.md
@.planning/phases/01.2-logo-generation/01.2-01-SUMMARY.md
</context>

<interfaces>
<!-- Key interfaces from Plan 01 output that this plan depends on. -->
<!-- Executor: use these directly — no codebase exploration needed. -->

From src/components/ForgeLogo.tsx (created in Plan 01):
```tsx
interface ForgeLogoProps {
  variant?: 'horizontal' | 'icon';
  className?: string;
}
export function ForgeLogo({ variant = 'horizontal', className = 'h-7 w-auto' }: ForgeLogoProps)
```

Current nav markup being replaced (identical in both trainer and trainee layouts):
```tsx
// BEFORE — lines 8-10 in (trainer)/layout.tsx and (trainee)/layout.tsx
<Link href="/trainer" className="font-bold text-accent text-lg hover:text-accent-hover transition-colors">
  ⚡ Forge
</Link>
```

Current auth page structure (login — same pattern for both signup pages):
```tsx
// BEFORE — login/page.tsx (entire component)
export default function LoginPage() {
  return (
    <div className="bg-bg-surface border border-border rounded-sm p-8 space-y-6">
      <div>
        <h1 className="text-2xl font-bold text-text-primary">Sign in</h1>
        ...
      </div>
      ...
    </div>
  );
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Replace ⚡ Forge in trainer and trainee nav layouts</name>
  <files>
    src/app/(trainer)/layout.tsx
    src/app/(trainee)/layout.tsx
  </files>
  <action>
Update both layout files to replace the `⚡ Forge` Link with the ForgeLogo component.

**In `src/app/(trainer)/layout.tsx`:**
1. Add import at top: `import { ForgeLogo } from '@/components/ForgeLogo';`
2. Replace lines 8-10:
   ```tsx
   // REMOVE:
   <Link href="/trainer" className="font-bold text-accent text-lg hover:text-accent-hover transition-colors">
     ⚡ Forge
   </Link>

   // REPLACE WITH:
   <Link href="/trainer" aria-label="Forge home">
     <ForgeLogo variant="horizontal" className="h-7 w-auto" />
   </Link>
   ```
   Note: The old `className` on the `<Link>` goes away entirely — the logo SVG carries its own colors. The `aria-label` on the `<Link>` provides accessible label since the link content is now an image, not text.

**In `src/app/(trainee)/layout.tsx`:**
Identical surgery. Replace the `<Link href="/trainee" ...>⚡ Forge</Link>` with:
```tsx
<Link href="/trainee" aria-label="Forge home">
  <ForgeLogo variant="horizontal" className="h-7 w-auto" />
</Link>
```
Add the same `ForgeLogo` import.

**Important:** The `font-bold text-accent text-lg hover:text-accent-hover transition-colors` classes on the old Link are removed completely — they were text styling for the placeholder, not nav styling. The Link wrapper has no className now (or minimal one if needed for layout alignment — check if the header flex layout still aligns correctly without it).
  </action>
  <verify>
    <automated>grep -L "⚡ Forge" "/Users/piotrsowinski/sources/Private/GYM AI ASSISTANT/src/app/(trainer)/layout.tsx" "/Users/piotrsowinski/sources/Private/GYM AI ASSISTANT/src/app/(trainee)/layout.tsx" | wc -l | tr -d ' ' && echo "files have no ⚡ Forge (expected: 2)"</automated>
  </verify>
  <done>
    Both layout files import `ForgeLogo` and render `&lt;ForgeLogo variant="horizontal" className="h-7 w-auto" /&gt;` inside the nav `<Link>`. The `⚡ Forge` text is gone from both files. TypeScript compiles without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add logo above cards on login and signup auth pages</name>
  <files>
    src/app/(auth)/login/page.tsx
    src/app/(auth)/signup/trainer/page.tsx
    src/app/(auth)/signup/trainee/page.tsx
  </files>
  <action>
Add the horizontal ForgeLogo above the dark surface card on all three auth pages.

**Pattern for each page (from RESEARCH.md — auth layout centering pitfall):**
- Return a React fragment (`<>`) or a plain div with `space-y-6` — do NOT add extra centering wrappers
- The auth layout already handles `min-h-screen flex items-center justify-center` with `max-w-md` — adding another centering wrapper will double-center
- Logo goes ABOVE the existing card `<div className="bg-bg-surface ...">`

**`src/app/(auth)/login/page.tsx`:**
```tsx
import { ForgeLogo } from '@/components/ForgeLogo';
import { LoginForm } from './_components/LoginForm';

export default function LoginPage() {
  return (
    <>
      <div className="flex justify-center mb-8">
        <ForgeLogo variant="horizontal" className="h-10 w-auto" />
      </div>
      <div className="bg-bg-surface border border-border rounded-sm p-8 space-y-6">
        <div>
          <h1 className="text-2xl font-bold text-text-primary">Sign in</h1>
          <p className="text-sm text-text-primary mt-1">Welcome back</p>
        </div>
        <LoginForm />
        <p className="text-center text-sm text-text-primary">
          New here?{' '}
          <a href="/signup/trainer" className="text-accent hover:text-accent-hover">Trainer signup</a>
          {' '}·{' '}
          <a href="/signup/trainee" className="text-accent hover:text-accent-hover">Trainee signup</a>
        </p>
      </div>
    </>
  );
}
```

**`src/app/(auth)/signup/trainer/page.tsx`:**
Same pattern — add `ForgeLogo` import, wrap in fragment, add logo div above the existing card div. Keep all existing card content (h1, SignupForm, sign-in link) unchanged.

**`src/app/(auth)/signup/trainee/page.tsx`:**
Same pattern. Read the file first to confirm exact card content, then apply identical transformation.

**Logo sizing:** Use `className="h-10 w-auto"` on auth pages (slightly larger than nav's `h-7` — auth pages have more breathing room).
  </action>
  <verify>
    <automated>grep -l "ForgeLogo" "/Users/piotrsowinski/sources/Private/GYM AI ASSISTANT/src/app/(auth)/login/page.tsx" "/Users/piotrsowinski/sources/Private/GYM AI ASSISTANT/src/app/(auth)/signup/trainer/page.tsx" "/Users/piotrsowinski/sources/Private/GYM AI ASSISTANT/src/app/(auth)/signup/trainee/page.tsx" | wc -l | tr -d ' ' && echo "auth pages with ForgeLogo (expected: 3)"</automated>
  </verify>
  <done>
    All three auth pages import `ForgeLogo` and render `&lt;ForgeLogo variant="horizontal" className="h-10 w-auto" /&gt;` above the dark card. The existing card content (headings, forms, links) is unchanged. TypeScript compiles without errors (`npx tsc --noEmit`).
  </done>
</task>

</tasks>

<verification>
Run the following after both tasks complete:

1. TypeScript compile check:
   ```bash
   npx tsc --noEmit
   ```
   Expected: Zero errors

2. No ⚡ emoji remaining in layout files:
   ```bash
   grep -r "⚡" src/app/\(trainer\)/layout.tsx src/app/\(trainee\)/layout.tsx
   ```
   Expected: No output

3. All 5 integration files contain ForgeLogo:
   ```bash
   grep -l "ForgeLogo" \
     "src/app/(trainer)/layout.tsx" \
     "src/app/(trainee)/layout.tsx" \
     "src/app/(auth)/login/page.tsx" \
     "src/app/(auth)/signup/trainer/page.tsx" \
     "src/app/(auth)/signup/trainee/page.tsx"
   ```
   Expected: All 5 files listed

4. Dev server runs without errors: `npm run dev`
</verification>

<success_criteria>
- All 5 integration files use `ForgeLogo` — zero occurrences of `⚡ Forge` remaining
- Nav logo renders in both trainer and trainee layouts at `h-7` size
- Auth pages show logo above card at `h-10` size
- TypeScript compiles clean
- `npm run dev` starts without errors
- Phase 01.2 Logo Generation is complete
</success_criteria>

<output>
After completion, create `.planning/phases/01.2-logo-generation/01.2-02-SUMMARY.md`
</output>
